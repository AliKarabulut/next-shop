import HeaderContainer from "./container";
import HeaderTopMenu from "./topMenu";

const Header = () => {
  return (
    <header>
      <HeaderTopMenu />
      <HeaderContainer />
    </header>
  );
};

export default Header;
